{% set count = 1 %}
{% for image in images %}
    <img src="{{ web_dir }}/{{ image }}" alt="{{ image }}" style="width:236px;{% if count > 1 %}display:none;{% endif %}" />
    {% set count = count + 1 %}
{% endfor %}